
h2. e e' vicino al comandante di plotone, e poi le squadre di quel plotone, non roba di altri plotoni. e' basato sulla viewer camera   +187 o +1 o +2 non fa grande differenza


- Possiamo assumere che tutti i comandanti, ad ogni livello, abbiano il gps? Come gestire un coman

3. logica di decluttering scritte, legata a 2 ma piu' stringente, non dipende (SOLO o DEL TUTTO) dalla vicinanza tra unita', ma forse SOLO dalla distanza di camera. o meglio distanza in screen space. le scritte appaiono dopo (in meno casi) delle billboard

scritte si potrebbero anche spostare, a differenza dei pin

- Immaginiamo che una squadra abbia 4 individui con gps e 4 senza. Come gestiamo questa cosa?
- Forzare la continuita' della heatmap in modo che tutti siano parte di un unico blob crea dei casi ambigui. Nel caso le sottoparti siano molto distanti tra loro possiamo accettare la separazione in piu' chiazze?
- Immaginiamo uno scenario di uso pesante di Argo. Che estensione potrebbe avere lo scenario? 5km x 5km? 10km x 10km ? 20km x 20km? ...? 
- Quante persone ci sarebbero in questo scenario? 100? 1000? 5000? ...?

- sembra che AR debba essere 1.0 per il draping

- gps puo' avere svarioni e mandare un commander in africa
- assumiamo che tutti abbiano la radio+gps, se i commander non la avessero, in particolare, sarebbe un casino
- come rappresento il fatto che una squadra ha 7 persone senza gps?

Here are the most interesting options, roughly ranked:

  plan_path — Claude chooses start + end coordinates (using get_entities to find unit positions or waypoints) and tells the client to run the
  existing A* terrain pathfinder. The result is a terrain-hugging LIME route on the map. This is the most compelling because Claude's
  reasoning drives a computationally heavy operation that already exists. Prompt: "Route 1 Plt to waypoint B3, avoid exposed terrain".

  draw_circle — Claude places a circle entity on the map: threat zones, LZ coverage, exclusion areas, observation post radii. Pure Cesium
  primitive, no server side, very easy. Prompt: "Mark a 500m threat zone around the enemy position I'm looking at".

  add_waypoints — Claude places a sequence of pins on the map forming a route. Less interesting than plan_path because it's just coordinates,
  not terrain-aware movement.

  set_level — Claude switches between squad/platoon/company/battalion display. Trivial (one function call), only useful as part of a compound
  action.

  move_unit — Claude repositions a military unit. Most complex: requires mutating the JSON tree on disk and reloading client entities.
